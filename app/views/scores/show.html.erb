
<p>
  <strong>Student:</strong>
  <%= @score.student.first_name + " " + @score.student.last_name if @score.student %>
</p>

<p>
  <strong>Subject:</strong>
  <%= @score.subject.name if @score.subject_id%>
</p>

<p>
  <strong>Final Score:</strong>
  <%= @score.score %>
</p>

<%= link_to 'Edit', edit_score_path(@score), class: "btn btn-secondary" %> |
<%= link_to 'Back', scores_path, class: "btn btn-dark" %>
