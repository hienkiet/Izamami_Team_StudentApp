
<p>
  <strong>Student:</strong>
  <%= @score.student.first_name if @score.student %>
</p>

<p>
  <strong>Subject:</strong>
  <%= @score.subject.name if @score.subject_id%>
</p>

<p>
  <strong>Score:</strong>
  <%= @score.score %>
</p>

<%= link_to 'Edit', edit_score_path(@score) %> |
<%= link_to 'Back', scores_path %>
